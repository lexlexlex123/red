/**
 * ╔══════════════════════════════════════════════════╗
 * ║  config/export.js — Конфигурация экспорта        ║
 * ╚══════════════════════════════════════════════════╝
 */

window.CFG_EXPORT = {

  // ── HTML-экспорт ─────────────────────────────────────────────
  html: {
    // Имя файла по умолчанию (null = имя презентации)
    filename: null,

    // Встраивать шрифты в экспорт?
    embedFonts: false,

    // Показывать прогресс-бар в экспорте?
    showProgressBar: true,
  },

  // ── PPTX-импорт ──────────────────────────────────────────────
  pptx: {
    // Импортировать изображения?
    importImages: true,

    // Импортировать текст?
    importText: true,

    // Импортировать таблицы?
    importTables: true,

    // Максимальный размер PPTX-файла (байт, 0 = без ограничений)
    maxFileSizeBytes: 0,
  },

};
