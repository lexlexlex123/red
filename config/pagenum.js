/**
 * ╔══════════════════════════════════════════════════╗
 * ║  config/pagenum.js — Конфигурация нумерации      ║
 * ╚══════════════════════════════════════════════════╝
 */

window.CFG_PAGENUM = {

  // ── Включить нумерацию при старте? ────────────────────────────
  enabled: false,

  // ── Стиль: 'simple' | 'circle' | 'ring' | 'pill' | 'line' | 'dot'
  style: 'simple',

  // ── Позиция: 'tl' | 'tc' | 'tr' | 'bl' | 'bc' | 'br'
  position: 'br',

  // ── Шрифт ────────────────────────────────────────────────────
  fontSize: 14,

  // ── Прозрачность (0–1) ────────────────────────────────────────
  opacity: 1,

  // ── Показывать итого (1 / 5) ─────────────────────────────────
  showTotal: false,

  // ── Цвет (если customColor = false — берётся из темы) ────────
  customColor: false,
  color: '#3b82f6',
  textColor: '#ffffff',

};
