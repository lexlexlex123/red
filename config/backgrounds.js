/**
 * ╔══════════════════════════════════════════════════╗
 * ║  config/backgrounds.js — Конфигурация фонов      ║
 * ╚══════════════════════════════════════════════════╝
 *
 * Управляет набором пресетных фонов слайда.
 * Каждый объект: { id, label, style }
 *   id    — уникальный идентификатор
 *   label — подсказка (tooltip)
 *   style — CSS-значение background
 */

window.CFG_BACKGROUNDS = {

  // ── Заменить встроенные фоны? ─────────────────────────────────
  replaceBuiltin: false,

  // ── Фон по умолчанию для нового слайда ───────────────────────
  // Должен совпадать с id одного из фонов (встроенного или custom)
  defaultBgId: 'b1',

  // ── Дополнительные пресеты фонов ─────────────────────────────
  custom: [
    // { id: 'my1', label: 'Sunset', style: 'linear-gradient(135deg,#ff6b6b,#ffd93d)' },
    // { id: 'my2', label: 'Midnight Blue', style: '#0d0d2b' },
  ],

};
