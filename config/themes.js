/**
 * ╔══════════════════════════════════════════════════╗
 * ║  config/themes.js — Конфигурация тем оформления  ║
 * ╚══════════════════════════════════════════════════╝
 *
 * Добавляйте, удаляйте и редактируйте темы здесь.
 * Темы из этого файла ДОБАВЛЯЮТСЯ к встроенным темам
 * (или заменяют их, если установить replaceBuiltin: true).
 *
 * Поля темы:
 *   name          — название
 *   dark          — true = тёмная, false = светлая
 *   bg            — фон слайда (CSS цвет или градиент)
 *   ac1, ac2, ac3 — акцентные цвета (основной, вторичный, светлый)
 *   shapeFill     — заливка фигур
 *   shapeStroke   — обводка фигур
 *   headingColor  — цвет заголовков
 *   bodyColor     — цвет основного текста
 */

window.CFG_THEMES = {

  // ── Заменить встроенные темы этим файлом? ─────────────────────
  // false = добавить к встроенным (по умолчанию)
  // true  = использовать только темы из этого файла
  replaceBuiltin: false,

  // ── Тема по умолчанию (индекс в итоговом массиве) ─────────────
  // -1 = без темы при старте
  defaultThemeIndex: -1,

  // ── Дополнительные / кастомные темы ──────────────────────────
  // Добавляйте свои темы в этот массив:
  custom: [
    // Пример кастомной тёмной темы:
    // {
    //   name: 'My Dark',
    //   dark: true,
    //   bg: 'linear-gradient(135deg,#0a0a0a,#1a1a2e)',
    //   ac1: '#00d4ff', ac2: '#0099cc', ac3: '#80eaff',
    //   shapeFill: '#0099cc', shapeStroke: '#00d4ff',
    //   headingColor: '#00d4ff', bodyColor: '#e0f7ff',
    // },
    // Пример кастомной светлой темы:
    // {
    //   name: 'My Light',
    //   dark: false,
    //   bg: '#fafafa',
    //   ac1: '#4f46e5', ac2: '#6366f1', ac3: '#a5b4fc',
    //   shapeFill: '#6366f1', shapeStroke: '#4f46e5',
    //   headingColor: '#3730a3', bodyColor: '#1e293b',
    // },
  ],

};
