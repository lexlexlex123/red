/**
 * ╔══════════════════════════════════════════════╗
 * ║  config/canvas.js — Конфигурация холста      ║
 * ║  Независимый модуль. Если файл сломан —      ║
 * ║  приложение использует значения по умолчанию ║
 * ╚══════════════════════════════════════════════╝
 *
 * Параметры применяются при старте через boot().
 * Меняйте значения здесь и перезагружайте страницу.
 */

window.CFG_CANVAS = {

  // ── Размер слайда ──────────────────────────────────────────────
  width:  1200,   // ширина холста в пикселях
  height:  675,   // высота холста (675 = 16:9, 900 = 4:3)
  aspectRatio: '16:9',  // '16:9' | '4:3'

  // ── Сетка и привязка ──────────────────────────────────────────
  snapStep: 10,           // шаг привязки к сетке (px)
  snapEnabled: true,      // включить привязку по умолчанию
  guidesEnabled: true,    // показывать направляющие при перемещении
  guideThreshold: 7,      // порог срабатывания направляющей (px)

  // ── Внешний вид ───────────────────────────────────────────────
  gridDotColor: {
    dark:  'rgba(180,180,200,0.18)',
    light: 'rgba(80,80,120,0.25)',
  },

};
