/**
 * ╔══════════════════════════════════════════════════╗
 * ║  config/ui.js — Конфигурация интерфейса           ║
 * ╚══════════════════════════════════════════════════╝
 */

window.CFG_UI = {

  // ── Тема интерфейса: 'dark' | 'light'
  // (можно переключать в ⚙ Параметры во время работы)
  theme: 'dark',

  // ── Язык: 'ru' | 'en' ────────────────────────────────────────
  language: 'ru',

  // ── Toast-уведомления ────────────────────────────────────────
  toastDuration: 3600,  // мс

  // ── Вкладки: скрыть ненужные ──────────────────────────────────
  // Перечислите id вкладок которые хотите скрыть:
  hideTabs: [
    // 'home', 'insert', 'design', 'anim', 'transitions', 'slideshow', 'file'
  ],

  // ── Приветственный toast при старте ───────────────────────────
  showWelcomeToast: true,

  // ── Превью-миниатюры ──────────────────────────────────────────
  thumbnails: {
    width:  160,
    height:  90,
    // Принудительно перерисовывать все при каждом действии?
    // false = умный кэш (быстрее)
    forceRedrawAll: false,
  },

  // ── Undo/Redo ─────────────────────────────────────────────────
  undoMaxSteps: 40,

};
